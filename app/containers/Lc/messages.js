/*
 * Lc Messages
 *
 * This contains all the text for the Lc container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Lc';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Lc container!',
  },
});
